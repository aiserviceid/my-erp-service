name: landing_build_check

on:
  push:
    branches:
      - agent/landing-page-sales-polish
    paths:
      - '.github/workflows/temp-landing-build.yml'
      - '.github/landing-build-trigger.txt'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/landing-page-sales-polish
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Install dependencies
        run: npm ci
      - name: Build app
        run: npm run build
      - name: Cleanup temporary workflow
        if: always()
        run: |
          git config user.name "aiserviceid"
          git config user.email "ai.interiorid@gmail.com"
          git rm --ignore-unmatch .github/workflows/temp-landing-build.yml .github/landing-build-trigger.txt
          if git diff --cached --quiet; then
            echo "No cleanup changes"
            exit 0
          fi
          git commit -m "chore: cleanup landing build workflow"
          git pull --rebase origin agent/landing-page-sales-polish
          git push origin HEAD:agent/landing-page-sales-polish
