name: Dashboard Batch 3 Revision Build

on:
  push:
    branches:
      - agent/v1-stabilization-integration
    paths:
      - '.github/dashboard-batch3-revision-trigger.txt'

jobs:
  build-clean:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Build
        run: npm run build
      - name: Clean temp workflow
        run: |
          git config user.name "aiserviceid"
          git config user.email "ai.interiorid@gmail.com"
          git rm .github/workflows/temp-dashboard-batch3-revision.yml .github/dashboard-batch3-revision-trigger.txt
          git commit -m "chore: clean dashboard revision temp workflow" || echo "nothing to commit"
          git push
